# Session Notes — 2026-06-17

## What was done this session

### Exp01 (GTrXL gating) — completed and KEPT

- Training completed at ep100000; best checkpoint at ep65000 (`out_exp_gtrxl_best.pt`)
- Authoritative eval (`eval_only.py --episodes 10000 --seed 42`):
  ```
  ZI   mean PnL = +1253.59  sd=2423.76  sem=24.24
  TRON mean PnL = +1455.96  sd=2503.66  sem=25.04
  delta         = +202.37  sem=34.85  z=5.81
               = +16.1% over ZI
  ```
- Previous best: +9.7% (z=3.50). Improvement: +6.4 pp. GTrXL also beats LSTM (+12.5%).
- Decision: **KEEP** — merged `exp/gtrxl-gating` to master.
- Master pushed to origin/master.

### Exp02 (context-encoder-specific LR) — training started

- Branch: `exp/encoder-lr`
- Implementation: split Adam optimizer into encoder (input_proj + gated Transformer) at
  `lr * 0.1 = 1e-5` and heads at full `lr = 1e-4`. New `TrainerConfig.encoder_lr_scale`
  field + `--encoder-lr-scale` CLI arg.
- Training started in screen session `train_encoder_lr` at ~2026-06-17T13:5x
- Log: `research/experiments/exp02_encoder_lr_train.log`
- Expected completion: ~14-15 hours (same rate as exp01)

## When exp02 training completes

### Check training output
```bash
tail -10 research/experiments/exp02_encoder_lr_train.log
```

### Run the evaluation
```bash
PYTHONPATH=pymarketsim ./.venv/bin/python -u -m marketsim.tron.eval_only \
  --checkpoint out_exp_encoder_lr_best.pt --episodes 10000 --seed 42
```

### After eval: update research/log.md
```
| exp02_encoder_lr | encoder-specific LR (scale=0.1, GTrXL arch) | 16.1% | <new %> | <z> | 2026-06-18 | keep/discard |
```

### Decision and next steps

**If result > 16.1% (current GTrXL best):**
- Update `research/log.md` with decision: keep
- Update `research/baseline_metrics.md` with new best
- Update `RESEARCH.md` "Current best Transformer result" line
- Commit changes to `exp/encoder-lr`, merge to master, push
- Branch `exp/max-seq-128` from master
- Train with: `--arch gated-transformer --xfmr-max-seq 128 --encoder-lr-scale 0.1`
  (combine with encoder-lr if kept, otherwise just max-seq on GTrXL)

**If result ≤ 16.1% (no improvement):**
- Update `research/log.md` with decision: discard
- Do NOT merge
- Consecutive non-improving count: 1 (Phase 2 trigger at 3)
- Branch `exp/max-seq-128` from master (GTrXL arch, standard LR)

### Exp03: max_seq=128 (either outcome)
Branch: `exp/max-seq-128`  
Hypothesis: `research/experiments/exp03_max_seq_128_hypothesis.md`  
Training command:
```bash
screen -dmS train_max_seq bash -c "
PYTHONPATH=/path/to/pymarketsim caffeinate -i ./.venv/bin/python -u -m marketsim.tron.train \
  --arch gated-transformer \
  --xfmr-max-seq 128 \
  --episodes 100000 --eps-decay 40000 --buffer-size 50000 \
  --lr 1e-4 --encoder-lr-scale 0.1 \  # include only if exp02 kept
  --target-tau 0.005 --reward-clip 5.0 \
  --checkpoint out_exp_max_seq_final.pt --best-checkpoint out_exp_max_seq_best.pt \
  2>&1 | tee research/experiments/exp03_max_seq_128_train.log
echo 'TRAINING DONE' >> research/experiments/exp03_max_seq_128_train.log
"
```
If exp02 discarded, drop `--encoder-lr-scale 0.1`.

### After 3 cycles complete: write checkpoint_1.md
Follow the template in RESEARCH.md. Push to master. Do NOT start exp04 without human approval.

## Resume prompt for new Claude Code session

```
Continue the TRONformer research workflow defined in RESEARCH.md.
Current best Transformer checkpoint: out_exp_gtrxl_best.pt at 16.1% over ZI (z=5.81).
Phase: 1. Consecutive non-improving iterations: 0.
Experiment 02 (encoder-lr) training is complete; checkpoint at out_exp_encoder_lr_best.pt.
Run eval_only.py on out_exp_encoder_lr_best.pt (see research/session_2026-06-17.md for commands),
update research/log.md and research/experiments/exp02_encoder_lr_hypothesis.md with results,
then decide keep/discard, run exp03 (max_seq=128) per session notes, and write
research/checkpoint_1.md after exp03 completes (cycle 3 of 3).
Note: git push works (origin = kshinde6804/tronformer).
```
